default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: inbucket_cli_startos_development

test:
  <<: *default
  database: inbucket_cli_startos_test

production:
  <<: *default
